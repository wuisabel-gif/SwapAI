#!/bin/sh

if [ -n "${SWAPAI_TEST_CAPTURE:-}" ]; then
    printf 'curl|%s\n' "$*" >> "$SWAPAI_TEST_CAPTURE"
fi

case " $* " in
    *" -w "*) printf '0.012' ;;
    *"/v1/chat/completions"*)
        printf '%s\n' '{"choices":[{"message":{"content":"fixture response"}}]}'
        ;;
esac
exit 0
